# modernBERT_pretraining

Two separate files: 
- pretrain_tokenizer.py
- pretrain_model.py

`--data_folder` needs to point to the zipped Reuters files. The column "Body" is assumed to contain the text for pretraining.